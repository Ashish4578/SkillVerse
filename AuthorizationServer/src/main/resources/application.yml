server:
  port: 8087

logging:
  level:
    org.springframework.security: DEBUG

jwt:
  secret: 8f7c9a1e2b3d4f5g6h7j8k9l0m1n2o3p
  expiration: 900000

spring:
  application:
    name: auth-server

  datasource:
    url: jdbc:mysql://localhost:3306/skillverse_auth_server
    username: root
    password: Sql#1541
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect